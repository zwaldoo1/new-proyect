<nav class="navbar">
  <div class="navbar-container">
    <a href="/" class="brand-logo transition duration-300 hover:brightness-110">
      <img src="/images/logo111.png" alt="Logo" class="h-10 w-auto" />
    </a>
    

    <!-- Enlaces del navbar en la versión de escritorio -->
    <ul class="nav-links">
      <li><a href="/">Inicio</a></li>
      <li><a href="/equipos">Equipos</a></li>
      <li><a href="/reglas">Reglas</a></li>
      <li><a href="/inscripcion">Inscripcion</a></li>
      <li><a href="/contacto">Contacto</a></li>
    </ul>

    <!-- Menú hamburguesa -->
    <button class="hamburger" onclick="toggleMenu()">☰</button>
  </div>

  <!-- Menú móvil -->
  <ul class="mobile-menu">
    <li><a href="#home">Inicio</a></li>
    <li><a href="#features">Características</a></li>
    <li><a href="#pricing">Precios</a></li>
    <li><a href="#about">Acerca de</a></li>
    <li><a href="#contact">Contacto</a></li>
  </ul>
</nav>

<script>
  // Manejador para mostrar/ocultar el menú móvil
  function toggleMenu() {
    const menu = document.querySelector('.mobile-menu');
    menu.classList.toggle('active');
  }
</script>

<style>
  /* Global reset */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }


  /* Navbar */
  .navbar {
    background: linear-gradient(135deg, #00bfff, #ff7f50); /* Gradiente elegante */
    color: #fff;
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    padding: 10px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
  }

  /* Contenedor principal del navbar */
  .navbar-container {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
  }

  /* Logo */
  .brand-logo {
    font-size: 2rem;
    font-weight: 600;
    text-decoration: none;
    color: #fff;
    text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.3);
    transition: color 0.3s ease;
    flex-grow: 1; /* Permite que el logo ocupe el espacio disponible */
  }

  .brand-logo:hover {
    color: #ec9410;
  }

  /* Estilo para los enlaces de navegación en la versión de escritorio */
  .nav-links {
    list-style: none;
    display: flex;
    gap: 30px;
    flex-grow: 2; /* Evita que los enlaces se apilen o se superpongan */
    justify-content: flex-end; /* Alinea los enlaces a la derecha */
  }

  .nav-links li {
    position: relative;
  }

  .nav-links li a {
    text-decoration: none;
    color: #fff;
    font-size: 1.1rem;
    font-weight: 500;
    padding: 10px 15px;
    border-radius: 5px;
    text-transform: uppercase;
    transition: color 0.3s, transform 0.3s;
  }

  .nav-links li a:hover {
    color: #ffeb3b;
    transform: scale(1.05);
  }

  /* Estilo del menú móvil */
  .mobile-menu {
    display: none;
    position: absolute;
    top: 60px;
    left: 0;
    right: 0;
    background-color: #333;
    padding: 20px;
    flex-direction: column;
    gap: 15px;
    transition: transform 0.3s ease-in-out;
    transform: translateY(-100%);
  }

  .mobile-menu.active {
    transform: translateY(0);
  }

  .mobile-menu li {
    list-style: none;
  }

  .mobile-menu li a {
    color: #fff;
    font-size: 1.2rem;
    padding: 12px;
    text-decoration: none;
    transition: color 0.3s, background-color 0.3s;
  }

  .mobile-menu li a:hover {
    color: #ffeb3b;
    background-color: rgba(255, 255, 255, 0.2);
  }

  .hamburger {
    display: none;
    background: transparent;
    border: none;
    font-size: 2.5rem;
    color: #fff;
    cursor: pointer;
  }

  /* Estilos para pantallas pequeñas */
  @media (max-width: 768px) {
    .nav-links {
      display: none;
    }

    .hamburger {
      display: block;
      position: absolute;
      right: 20px; /* Mueve el botón hamburguesa hacia la derecha */
    }

    .mobile-menu {
      display: flex;
    }
  }
</style>
