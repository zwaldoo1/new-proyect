---
title: "Contacto"
import '../styles/global.css';
import Footer from '../components/Footer.astro';
import BaseLayout from '../layouts/BaseLayout.astro';
---
<styles/>
<BaseLayout/>
<main class="bg-gray-50 min-h-screen py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-4xl mx-auto bg-white p-8 rounded-xl shadow-xl">
    <h2 class="text-3xl font-semibold text-center text-blue-600">¡Contáctanos!</h2>
    <p class="mt-2 text-center text-gray-600">Si tienes alguna pregunta o duda, no dudes en escribirnos. Estaremos encantados de ayudarte.</p>

    <form action="#" method="POST" class="space-y-6 mt-8">
      <!-- Nombre -->
      <div>
        <label for="nombre" class="block text-lg font-medium text-gray-700">Nombre</label>
        <input
          type="text"
          name="nombre"
          id="nombre"
          placeholder="Tu nombre"
          class="mt-2 p-3 border border-gray-300 rounded-lg w-full focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />
      </div>

      <!-- Correo Electrónico -->
      <div>
        <label for="email" class="block text-lg font-medium text-gray-700">Correo Electrónico</label>
        <input
          type="email"
          name="email"
          id="email"
          placeholder="Tu correo electrónico"
          class="mt-2 p-3 border border-gray-300 rounded-lg w-full focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        />
      </div>

      <!-- Mensaje -->
      <div>
        <label for="mensaje" class="block text-lg font-medium text-gray-700">Mensaje</label>
        <textarea
          name="mensaje"
          id="mensaje"
          placeholder="Escribe tu mensaje"
          class="mt-2 p-3 border border-gray-300 rounded-lg w-full h-32 focus:outline-none focus:ring-2 focus:ring-blue-500"
          required
        ></textarea>
      </div>

      <!-- Enviar -->
      <div class="text-center">
        <button
          type="submit"
          class="w-full bg-blue-600 text-white font-semibold py-3 rounded-lg hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500"
        >
          Enviar Mensaje
        </button>
      </div>
    </form>
  </div>
</main>
<Footer/>